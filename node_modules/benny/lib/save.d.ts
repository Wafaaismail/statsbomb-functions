import { Suite } from 'benchmark';
import { SaveOptions } from './internal/common-types';
declare type Save = (options?: SaveOptions) => Promise<(suiteObj: Suite) => Suite>;
declare const save: Save;
export { save, Save };
export default save;
